# Planning docs are authored by hand and reviewed as prose (the schema included — it lives
# under docs/). Prettier reflows them into noisy diffs that bury real changes.
docs/
CLAUDE.md

dist/
coverage/
node_modules/
package-lock.json

# Subagent-driven-development scratch (gitignored)
.superpowers/
